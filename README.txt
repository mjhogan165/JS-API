A website that fetches and displays real data from an API built with HTML, CSS and JavaScript. (No third party libraries used)

Features:
-User can add and remove these items from a shopping list
-Dynamically created HTML based on the response from the API
-Toggle function that sorts the items alphabetically (A-Z) and vice versa (Z-A).
-Responsive design across desktop, ipad/tablet and mobile phones.
